// 18.ProcessEnd.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include <iostream>

// 

int main()
{
    // 하나의 의미를 가진 코드가 종료되었다.
    // 여기까지 코드를 한묶으로 보고 실행해라.
    ;

    int a = 10;

    // int while = 20;

    // ;
    // }
    // }으로 끝날때는 굳이 끝낼때 ; 붙여줄 필요가 없다.

    // 대부분의 의미를 가진 코드 뒤에는 이게 붙는다.

    // => 문장부호라고 한다.

    if (true)
    {

    }

    if (false);



    {
        printf_s("실행됩니다.");
    }

    int Count = 100;

    while (Count);
    {
        --Count;
    }
}
